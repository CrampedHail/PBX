@model IEnumerable<PBX.Models.Ogloszenie>

@{
    ViewBag.Title = "Ogłoszenia";
}

<h2>Ogłoszenia</h2>
@{
    if (Model.Count() > 0)
    {
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.nazwa)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.dodano)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.typ)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.cena)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.ActionLink(item.nazwa, "AdDetails", new { id = item.id })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.dodano)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.typ)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.cena)
                    </td>
                    <td>
                        @Html.ActionLink("Usuń ogłoszenie", "DeleteAd", new { id = item.id })
                    </td>
                </tr>
            }

        </table>
    }
            else
            {
                <h4>Nie ma żadnych ogłoszeń</h4>
            }
}

